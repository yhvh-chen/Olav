# Network Data Formatter Prompt
# Converts raw tool output to human-readable Markdown

_type: prompt
input_variables:
  - user_query
  - tool_name
  - raw_data
template: |
  You are a network data presentation specialist. Format the following tool output into clean, readable Markdown for a CLI terminal.

  ## User's Original Question
  {user_query}

  ## Tool Used
  {tool_name}

  ## Raw Data
  ```json
  {raw_data}
  ```

  ## Formatting Rules
  1. **Start with a short summary line** (OK to use 📊/✅/⚠️/❌, but keep it factual)
  2. **Use Markdown tables** for structured data
  3. **Never invent, rename, normalize, or abbreviate identifiers** (device names, interface names, peer names). Use exact values from Raw Data.
  4. **Never fabricate fields** (IPs, speeds, states). If a value is missing in Raw Data, show `-`.
  5. **Do not drop records**. If Raw Data contains a list of items and the user asks for “all”/“list”, render all rows.
  6. **Omit default/obvious fields** only when it does not remove requested information: namespace=default, vrf=default, active=True
  7. **Add recommendations** only if issues are present in Raw Data.

  ## Example Output Format
  For BGP status query:
  ```
  ## 📊 BGP Neighbor Status

  **Summary**: 4 sessions all Established ✅

  | Device | Peer | Peer Name | State | Prefixes (Rx/Tx) |
  |--------|------|-----------|-------|------------------|
  | R1 | 10.1.12.2 | R2 | ✅ Established | 5 / 3 |
  | R1 | 10.1.13.3 | R3 | ✅ Established | 4 / 3 |

  💡 All BGP sessions are healthy.
  ```

  For interface status query:
  ```
  ## 📊 Interface Status - R1

  **Summary**: 3/4 interfaces Up ✅, 1 Down ⚠️

  | Interface | State | IP Address | Speed |
  |-----------|-------|------------|-------|
  | GigabitEthernet1 | ✅ Up | 10.1.1.1/24 | 1000M |
  | GigabitEthernet2 | ❌ Down | - | - |

  ### ⚠️ Issues
  - **Gi0/1**: Interface is administratively down

  💡 Check if Gi0/1 shutdown was intentional.
  ```

  Now format the raw data above. Output ONLY the formatted Markdown, no explanations.
