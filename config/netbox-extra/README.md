# NetBox Extra Configuration

This directory is mounted to NetBox container at `/etc/netbox/config/extra/`.

Use it for custom NetBox configuration:
- `extra.py`: Custom settings overrides
- Plugin configurations
- Custom field definitions
- Webhook handlers

See: https://docs.netbox.dev/en/stable/configuration/
