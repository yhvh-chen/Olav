# OLAV WebGUI Environment Variables

# API URL (backend server)
NEXT_PUBLIC_API_URL=http://localhost:8001

# App Configuration
NEXT_PUBLIC_APP_NAME=OLAV
NEXT_PUBLIC_DEFAULT_LOCALE=zh

# Development Token (optional - for skipping login)
# Generate with: uv run python scripts/generate_dev_token.py --env
# NEXT_PUBLIC_DEV_TOKEN=eyJhbGciOiJIUzI1NiIs...
