# OLAV minimal .env example
# Principle: keep ONLY secrets and environment-specific endpoints here.
# Non-sensitive defaults live in config/settings.py; missing values are auto-built based on environment.

# --- LLM (secret)
LLM_API_KEY=sk-xxxxxxxxxxxxxxxxxxxxxxxx
# Optional overrides (non-secret) — prefer defaults in config/settings.py
# LLM_PROVIDER=openai        # openai|ollama|azure (optional)
# LLM_MODEL_NAME=gpt-4-turbo # optional

# --- NetBox (secret + endpoint)
NETBOX_TOKEN=replace-with-your-netbox-token
NETBOX_URL=https://netbox.example.com   # Or omit to use Docker service URL inside containers

# --- Device credentials (secret)
DEVICE_USERNAME=admin
DEVICE_PASSWORD=change_me

# --- Infrastructure endpoints (optional)
# If omitted, they auto-resolve based on environment (local vs docker) using InfrastructureConfig
# POSTGRES_URI=postgresql://olav:OlavPG123!@localhost:55432/olav
# OPENSEARCH_URL=http://localhost:9200
# REDIS_URL=redis://localhost:6379

# --- Docker-only overrides (optional)
# These are set via docker-compose for containers; define here only if running services externally.
